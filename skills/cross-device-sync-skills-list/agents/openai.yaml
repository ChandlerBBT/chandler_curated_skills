interface:
  display_name: "Cross-device Sync Legacy"
  short_description: "Legacy alias for the newer cross-device-sync skill."
  default_prompt: "Use $cross-device-sync instead of this legacy skill."

